services:
  redis:
    image: redis:latest
    restart: always
    ports:
      - "6379:6379"

  db:
    image: supabase/postgres:15.8.1.045
    ports:
      - "5432:5432"
    command: postgres -c config_file=/etc/postgresql/postgresql.conf
    environment:
      POSTGRES_PASSWORD: postgres
